# ==================== FLEXBOARD BACKEND API - PRODUCTION DEPLOYMENT ====================
# This file deploys ONLY the backend API (Express.js) to Cloud Run
# Includes Firebase Admin SDK and database connectors

steps:
  # ==================== BUILD BACKEND (PRODUCTION) ====================
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-backend-production'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/flexboard-api-prod:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/flexboard-api-prod:latest'
      - '-f'
      - 'backend/Dockerfile'
      - 'backend'

  # ==================== PUSH IMAGE ====================
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-backend-production-sha'
    args: ['push', 'gcr.io/$PROJECT_ID/flexboard-api-prod:$COMMIT_SHA']
    waitFor: ['build-backend-production']

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-backend-production-latest'
    args: ['push', 'gcr.io/$PROJECT_ID/flexboard-api-prod:latest']
    waitFor: ['build-backend-production']

  # ==================== DEPLOY TO CLOUD RUN (PRODUCTION) ====================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-backend-production'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'flexboard-api-prod'
      - '--image'
      - 'gcr.io/$PROJECT_ID/flexboard-api-prod:$COMMIT_SHA'
      - '--region'
      - 'asia-southeast1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NODE_ENV=production,FIREBASE_PROJECT_ID=${_FIREBASE_PROJECT_ID},PORT=8080'
      - '--set-secrets'
      - 'SECRET_KEY=FLEXBOARD_SECRET_KEY_PROD:latest,FIREBASE_SERVICE_ACCOUNT=FLEXBOARD_FIREBASE_SERVICE_ACCOUNT_PROD:latest'
      - '--max-instances'
      - '1'
      - '--min-instances'
      - '0'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '2'
      - '--timeout'
      - '300'
      - '--concurrency'
      - '80'
      - '--tag'
      - 'production'
    waitFor: ['push-backend-production-sha']

# ==================== IMAGES ====================
images:
  - 'gcr.io/$PROJECT_ID/flexboard-api-prod:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/flexboard-api-prod:latest'

# ==================== BUILD OPTIONS ====================
options:
  logging: CLOUD_LOGGING_ONLY

# ✅ Global timeout setting
timeout: '1800s'

# ==================== SUBSTITUTIONS ====================
substitutions:
  _FIREBASE_PROJECT_ID: 'SET_IN_CLOUD_BUILD_TRIGGER'
