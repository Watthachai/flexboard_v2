# ==================== FLEXBOARD ADMIN FRONTEND - PRODUCTION DEPLOYMENT ====================
# This file deploys ONLY the admin frontend (Next.js) to Cloud Run
# Backend API URL must be set to point to flexboard-api-prod

steps:
  # ==================== BUILD FRONTEND (PRODUCTION) ====================
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-frontend-production'
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_BACKEND_URL=${_BACKEND_URL_PROD}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_API_KEY=${_FIREBASE_API_KEY}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${_FIREBASE_AUTH_DOMAIN}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_PROJECT_ID=${_FIREBASE_PROJECT_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${_FIREBASE_STORAGE_BUCKET}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${_FIREBASE_MESSAGING_SENDER_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_APP_ID=${_FIREBASE_APP_ID}'
      - '-t'
      - 'gcr.io/$PROJECT_ID/flexboard-admin-prod:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/flexboard-admin-prod:latest'
      - '-f'
      - 'frontend/Dockerfile'
      - 'frontend'

  # ==================== PUSH IMAGE ====================
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-frontend-production-sha'
    args: ['push', 'gcr.io/$PROJECT_ID/flexboard-admin-prod:$COMMIT_SHA']
    waitFor: ['build-frontend-production']

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-frontend-production-latest'
    args: ['push', 'gcr.io/$PROJECT_ID/flexboard-admin-prod:latest']
    waitFor: ['build-frontend-production']

  # ==================== DEPLOY TO CLOUD RUN (PRODUCTION) ====================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-frontend-production'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'flexboard-admin-prod'
      - '--image'
      - 'gcr.io/$PROJECT_ID/flexboard-admin-prod:$COMMIT_SHA'
      - '--region'
      - 'asia-southeast1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--max-instances'
      - '1'
      - '--min-instances'
      - '0'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '300'
      - '--concurrency'
      - '80'
      - '--tag'
      - 'production'
    waitFor: ['push-frontend-production-sha']

# ==================== IMAGES ====================
images:
  - 'gcr.io/$PROJECT_ID/flexboard-admin-prod:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/flexboard-admin-prod:latest'

# ==================== BUILD OPTIONS ====================
options:
  logging: CLOUD_LOGGING_ONLY

# ✅ Global timeout setting
timeout: '2400s'

# ==================== SUBSTITUTIONS ====================
substitutions:
  _BACKEND_URL_PROD: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_PROJECT_ID: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_API_KEY: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_AUTH_DOMAIN: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_STORAGE_BUCKET: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_MESSAGING_SENDER_ID: 'SET_IN_CLOUD_BUILD_TRIGGER'
  _FIREBASE_APP_ID: 'SET_IN_CLOUD_BUILD_TRIGGER'
